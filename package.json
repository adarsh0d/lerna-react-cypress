{
  "name": "@demo/root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  },
  "scripts": {
    "start": "lerna exec --scope @demo/components -- yarn start",
    "new-version": "lerna version --conventional-commits --yes"
  },
  "dependencies": {
    "react": "^17.0.2"
  }
}
